namespace Dreamine.Gem;

/// <summary>
/// \if KO
/// <para>Dreamine GEM 구현 어셈블리를 검색하거나 등록할 때 사용하는 표식입니다.</para>
/// \endif
/// \if EN
/// <para>Marks the assembly boundary used to discover or register Dreamine GEM implementations.</para>
/// \endif
/// </summary>
public sealed class GemAssemblyMarker
{
    /// <summary>\if KO GEM 어셈블리 표식을 만듭니다. \endif \if EN Creates a GEM assembly marker. \endif</summary>
    public GemAssemblyMarker() { }
}
